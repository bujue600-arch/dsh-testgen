## Summary

<!-- What does this PR change and why? Link the issue with `Closes #123`. -->

## Checklist

- [ ] `pnpm run typecheck` passes
- [ ] `pnpm run lint` passes
- [ ] `pnpm test` passes (new behavior has tests)
- [ ] `pnpm run build && pnpm run verify:manifest` passes
- [ ] Docs updated when the contract changed (`docs/io-spec.md`, `docs/config.md`, README)
- [ ] `CHANGELOG.md` updated under an Unreleased heading when user-visible

## Test plan

<!-- How did you verify the change? Commands, fixtures, edge cases. -->
